# Sistema de Pedido de Informações - Request-Reply com ZeroMQ

# Dados persistidos (não versionar)
data/
# Não ignorar package.json e package-lock.json
!package.json
!package-lock.json

# Python
__pycache__/
*.py[cod]  # Ignora .pyc, .pyo, .pyd (compilados), mas NÃO .py (fontes)
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
# requirements.txt deve estar versionado (não ignorar)

# Virtual environments
venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker
# .dockerignore deve estar versionado (removido da lista)
# Arquivos Docker devem estar versionados para funcionar em qualquer computador

# Logs
*.log

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# package-lock.json não deve ser ignorado (removido da lista)

# .NET
bin/
obj/
*.csproj.user  # Ignora arquivos de usuário, mas .csproj deve estar versionado
*.suo
*.user
# Arquivos .csproj devem estar versionados (não ignorar)